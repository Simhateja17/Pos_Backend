# Backend

Not yet scaffolded — Node.js + Express + TypeScript, per `INITIAL_PLAN.md` and `.planning/PROJECT.md`.

Will be built starting in Phase 1 of the roadmap (`.planning/ROADMAP.md`). See `INITIAL_PLAN.md` §6 for the intended architecture: Supabase (Postgres + Auth) for schema/RLS/migrations, Prisma as a typed query client only, append-only stock ledger, region-parameterized config.
