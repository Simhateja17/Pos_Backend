import { Router } from 'express'

const router = Router()

// Later plans in this phase mount their routers here, e.g.:
//   router.use('/auth', authRouter)      // 01-06
//   router.use('/members', membersRouter) // 01-07
//   router.use('/pin', pinRouter)         // 01-08

export default router
